# Scope Extension

This extension creates a scope for the MembershipCredential as it is needed by each DSP call.
Also, all custom credentials like the MarketPartnerCredential will be added to the scope, so they can be used in policies.