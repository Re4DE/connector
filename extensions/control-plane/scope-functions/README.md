# Scope Extension

This extension creates a scope for the MembershipCredential as it is needed by each DSP call.
Also, the extension registers the Data Space issuer as trusted issuer for Verifiable Credentials.